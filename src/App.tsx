import * as React from 'react';
import MainContainer from './components/containers/MainContainer';

const App: React.FC<{}> = () => {
  return (
    <div id='root'>
      <MainContainer />
    </div>
  );
};

export default App;
